# Admin Token (用于 API 认证)
ADMIN_TOKEN=change-me

# 加密密钥 (用于凭证加密，必须是 32 字节)
ENCRYPTION_KEY=change-me-32-bytes-key-here!!

# Webhook URL (用于通知，可选)
WEBHOOK_URL=

# CORS 允许的域名 (生产环境请设置具体域名，逗号分隔)
CORS_ORIGINS=*
